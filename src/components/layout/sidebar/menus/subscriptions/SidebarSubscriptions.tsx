import { SubscriptionItem } from "./SubscriptionItem";

export function SidebarSubscriptions() {
  return (
    <div>
      <ul>
        {/* {menu.map(menuItem => (
          <SubscriptionItem
            key={menuItem.label}
            item={menuItem}
          />
        ))} */}
      </ul>
    </div>
  )
}
